// Создание массива с элементами 5 и 4
var arr = [5, 4];
// Добавление элемента 'word' в массив arr
arr[2] = 'word';
// Вывод массива arr в консоль
/console.log(arr);
// Вывод длины массива arr в консоль
console.log(arr.length);

// Создание матрицы
var matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9],
];
// Вывод матрицы matrix в консоль
console.log(matrix);
